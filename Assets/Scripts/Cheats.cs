using UnityEngine;

public class Cheats : MonoBehaviour
{
    //stores cheats/debug tools - Nova
    //Coded by Nova

    public bool unlockAll;
    public bool enemyCounter;
    public bool hitStops;// this is more a test then a permanent feature, do not leave enabled in final build.
}
